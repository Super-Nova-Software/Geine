export enum ChannelType {
    TEXT = 'Text',
    NOTE = 'Note',
    AUDIO = 'Audio',
    VIDEO = 'Video',
  }
 export enum MemberRole {
   ADMIN = "Admin",
   MODERATOR = "Moderator",
   GUEST = "Guest",
 }